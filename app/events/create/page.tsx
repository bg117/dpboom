import CreateComponent from "@/components/createComponent";

export default function Events() {
    return <CreateComponent />;
}

export const metadata = {
    title: "Create | dpBoom!",
    description: "Create a new event on dpBoom!"
}